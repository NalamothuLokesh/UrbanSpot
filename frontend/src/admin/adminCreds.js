// Temporary admin credentials file for local development
// Do NOT commit secrets in production. This file is for convenience only.

const adminCreds = {
  email: 'admin@example.com',
  password: 'StrongP@ssw0rd!'
};

export default adminCreds;
